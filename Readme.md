#chai aur backend Series
model link
https://app.eraser.io/workspace/YtPqZ1VogxGy1jzIDkzj

# They are used for when folders are make in folder and that case they are used:--

.gitkeep

# They are used if want git ignore some files for push

( also write some ignore which we want that are not push
and websities are eg Toptal that are .gitignore generator )
.gitignore

---

When I we need to stop the server
npm i -D nodemon
